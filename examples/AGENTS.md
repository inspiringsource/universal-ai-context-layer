# AGENTS.md: universal-ai-context-layer-demo

> Generated by Universal AI Context Layer (UACL). Refresh with
> `aicontext generate && aicontext export`.

## Project

UACL is a context compiler that generates and validates AI-consumable project context.

## Goals

- Keep AGENTS.md and related context outputs aligned with repository changes.
- Preserve important human-authored decisions when generated analysis is refreshed.

## Instructions

- Read `.ai/context.yaml` before changing compiler behavior.
- Run `aicontext check` before publishing compiled outputs.
- Run tests, Ruff linting, and formatting checks before completing changes.

## Constraints

- Do not overwrite an existing root AGENTS.md without an explicit force flag.
- Keep existing CLI and package names backward compatible.

## Current Tasks

- Improve context-source ingestion and drift validation.

## Important Files

- `src/ai_context_map/cli.py`: CLI commands
- `src/ai_context_map/commands/generate_cmd.py`: canonical context generation
- `src/ai_context_map/emitter/portable_writer.py`: compiled outputs
